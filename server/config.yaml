# 服务器配置
server:
  port: "8080"
  mode: "debug"  # debug, release

# 数据库配置
database:
  host: "localhost"
  port: "3306"
  user: "root"
  password: ""
  name: "football_team"

# 日志配置
log:
  level: "info"    # debug, info, warn, error
  path: "/home/log"
  max_size: 10    # MB
  max_backups: 30  # 保留天数
  max_age: 30
  compress: true
